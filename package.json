{
  "name": "nedstack-bun-template",
  "version": "1.1.1",
  "module": "src/app.js",
  "devDependencies": {
    "@biomejs/biome": "^2.2.3",
    "bun-types": "latest"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "NODE_ENV=development bun --hot server.js",
    "compress": "bun run scripts/compress.js",
    "lint": "biome check",
    "format": "biome check --fix",
    "start": "NODE_ENV=production bun run server.js",
    "seed": "bun run database/seed.js"
  }
}
